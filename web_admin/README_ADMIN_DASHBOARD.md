# 🎯 QRChat 웹 관리자 대시보드

## 📋 개요

QRChat 앱의 QKEY 출금 신청을 PC 웹 브라우저에서 관리할 수 있는 관리자 대시보드입니다.

---

## 🚀 접속 방법

### 1️⃣ 로컬 서버 실행

```bash
# QRChat 프로젝트 디렉토리에서
python3 -m http.server 9000

# 또는 Node.js 사용 시
npx http-server -p 9000
```

### 2️⃣ 브라우저 접속

```
http://localhost:9000/web_admin/admin_dashboard.html
```

### 3️⃣ Google 계정으로 로그인

- "Google로 로그인" 버튼 클릭
- Firebase에 등록된 관리자 계정으로 로그인

---

## ✨ 주요 기능

### 📊 대시보드
- **실시간 통계**
  - 대기중 출금 수
  - 오늘 승인된 출금 수
  - 총 출금된 QKEY
  - 전체 사용자 수

### 💰 QKEY 출금 관리
- **4개 탭으로 구분**
  - 🟠 **대기중**: 사용자가 신청한 출금 목록
  - 🔵 **승인됨**: 관리자가 승인한 출금 (전송 대기중)
  - 🟢 **완료됨**: 실제 전송 완료된 출금
  - 🔴 **거부됨**: 거부된 출금 (잔액 복구 완료)

### 🔧 관리 작업
1. **출금 상세 보기**
   - 사용자 ID
   - 출금 금액
   - 지갑 주소 (클립보드 복사 가능)
   - 신청 시간
   - 거래 후 잔액

2. **승인 처리**
   - 출금 신청 승인
   - 관리자 메모 작성 (선택)
   - 상태: pending → approved

3. **거부 처리**
   - 출금 신청 거부
   - 거부 사유 작성 (필수)
   - 사용자 QKEY 잔액 자동 복구
   - 상태: pending → rejected

4. **완료 처리**
   - 실제 암호화폐 전송 후 완료 처리
   - 완료 메모 작성 (선택)
   - 상태: approved → completed

---

## 🔐 보안 설정

### Firebase 관리자 권한 설정

1. **Firebase Console** 접속
   ```
   https://console.firebase.google.com/
   ```

2. **Authentication** → **Users** 탭
   - 관리자로 지정할 사용자 확인

3. **Firestore Database** → **Rules** 설정
   ```javascript
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       // QKEY 트랜잭션 - 관리자만 수정 가능
       match /qkey_transactions/{transactionId} {
         allow read: if request.auth != null;
         allow write: if request.auth != null && 
                         request.auth.token.email in [
                           'admin@example.com',  // 실제 관리자 이메일로 변경
                           'your-email@gmail.com'
                         ];
       }
       
       // 사용자 데이터 - 본인 또는 관리자만 수정 가능
       match /users/{userId} {
         allow read: if request.auth != null;
         allow write: if request.auth != null && 
                         (request.auth.uid == userId || 
                          request.auth.token.email in [
                            'admin@example.com',
                            'your-email@gmail.com'
                          ]);
       }
     }
   }
   ```

---

## 📱 워크플로우

### 사용자 측 (모바일 앱)
1. 채팅 활동으로 QKEY 적립 (5분당 10 QKEY)
2. 1,000 QKEY 이상 보유 시 출금 신청
3. 지갑 주소 입력
4. 출금 신청 완료

### 관리자 측 (PC 웹)
1. 웹 대시보드 접속
2. "대기중" 탭에서 신청 확인
3. 출금 상세 정보 확인
   - 사용자 검증
   - 지갑 주소 확인
4. **승인** 클릭
5. 실제 암호화폐 지갑에서 전송
6. "승인됨" 탭에서 해당 출금 찾기
7. **완료 처리** 클릭

### 거부 시
1. "대기중" 탭에서 출금 확인
2. **거부** 클릭
3. 거부 사유 입력 (필수)
4. 자동으로 사용자 QKEY 잔액 복구

---

## 🎨 화면 구성

```
┌─────────────────────────────────────────────────┐
│  Sidebar               │  Main Content          │
│  ─────────            │  ─────────────         │
│  📊 대시보드          │  📊 대시보드            │
│  💰 QKEY 출금 관리    │                        │
│  🎨 스티커 관리       │  [통계 카드들]          │
│  👥 사용자 관리       │  ┌────┬────┬────┬────┐ │
│                       │  │ 10 │ 5  │1000│ 50 │ │
│                       │  └────┴────┴────┴────┘ │
│  [로그아웃]           │                        │
│                       │  [출금 신청 테이블]     │
│                       │  ┌──────────────────┐  │
│                       │  │ 대기중 │ 승인됨  │  │
│                       │  ├──────────────────┤  │
│                       │  │ 신청내역...      │  │
│                       │  └──────────────────┘  │
└─────────────────────────────────────────────────┘
```

---

## 🛠️ 기술 스택

- **프론트엔드**: HTML5, CSS3, Vanilla JavaScript
- **인증**: Firebase Authentication (Google)
- **데이터베이스**: Cloud Firestore
- **실시간 업데이트**: Firestore Realtime Listeners
- **모듈**: Firebase SDK v10.7.1 (ES Modules)

---

## 📊 Firestore 데이터 구조

### qkey_transactions 컬렉션
```javascript
{
  id: "auto-generated-id",
  userId: "user-uid",
  type: "withdraw",  // earn, withdraw, bonus, penalty
  amount: -1000,  // 출금은 음수
  balanceAfter: 500,
  timestamp: Timestamp,
  withdrawStatus: "pending",  // pending, approved, completed, rejected
  walletAddress: "0x1234...",
  adminNote: "승인함",
  adminId: "admin-uid",
  processedAt: Timestamp
}
```

### users 컬렉션
```javascript
{
  id: "user-uid",
  nickname: "사용자명",
  qkeyBalance: 1500,
  // ... 기타 필드
}
```

---

## ⚠️ 주의사항

### 보안
- ✅ 반드시 HTTPS 사용 (프로덕션)
- ✅ Firebase Rules에서 관리자 이메일 검증
- ✅ 실제 암호화폐 전송 전 충분한 검증
- ❌ 로컬호스트는 개발 환경에서만 사용

### 운영
- 💰 출금 "승인" ≠ 실제 전송 완료
- ✅ 실제 전송 후 반드시 "완료 처리" 클릭
- 🔴 거부 시 자동으로 잔액 복구되므로 신중히 결정
- 📝 승인/거부 시 메모 작성 권장

---

## 🚀 배포

### GitHub Pages (추천)
```bash
# web_admin 폴더를 gh-pages 브랜치로 배포
git subtree push --prefix web_admin origin gh-pages
```

접속 URL: `https://your-username.github.io/qrchat/admin_dashboard.html`

### Firebase Hosting
```bash
# Firebase CLI 설치
npm install -g firebase-tools

# 로그인
firebase login

# 초기화
firebase init hosting

# 배포
firebase deploy --only hosting
```

### 기타 호스팅
- Netlify
- Vercel
- AWS S3 + CloudFront
- Azure Static Web Apps

---

## 🐛 문제 해결

### 로그인이 안 돼요
- Firebase Console에서 Google 로그인 활성화 확인
- 브라우저 쿠키/캐시 삭제 후 재시도
- 관리자 이메일이 Firebase에 등록되었는지 확인

### 데이터가 안 보여요
- Firebase Console에서 Firestore 데이터 존재 확인
- 브라우저 개발자 도구(F12) Console 탭에서 에러 확인
- Firestore Rules 권한 확인

### 승인/거부가 안 돼요
- Firestore Rules에서 관리자 권한 확인
- 네트워크 연결 상태 확인
- 브라우저 콘솔에서 에러 메시지 확인

---

## 📞 지원

문제가 발생하거나 기능 추가가 필요하시면:
- GitHub Issues: https://github.com/Stevewon/qrchat/issues
- 개발자 연락처: [연락처 추가]

---

## 📝 라이선스

QRChat Project © 2026

---

**🎉 QRChat 관리자 대시보드 - PC에서 편리하게 관리하세요!**
